name: Merge All Serenity Reports

on:
  workflow_dispatch:
  workflow_run:
    workflows:
      - iOS Automation Tests Execution
      - Android Automation Tests Execution
      - Manual Tests Execution
    types:
      - completed

jobs:
  merge-reports:
    runs-on: ubuntu-latest

    env:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      # ==========================================
      # GET LATEST RUN IDS
      # ==========================================
      - name: Fetch workflow run IDs
        id: runids
        run: |

          echo "Fetching latest successful workflow runs..."

          IOS_RUN_ID=$(curl -s \
            -H "Authorization: Bearer $GITHUB_TOKEN" \
            "https://api.github.com/repos/${{ github.repository }}/actions/workflows/ios.yml/runs?status=success&per_page=1" \
            | jq -r '.workflow_runs[0].id')

          ANDROID_RUN_ID=$(curl -s \
            -H "Authorization: Bearer $GITHUB_TOKEN" \
            "https://api.github.com/repos/${{ github.repository }}/actions/workflows/android.yml/runs?status=success&per_page=1" \
            | jq -r '.workflow_runs[0].id')

          MANUAL_RUN_ID=$(curl -s \
            -H "Authorization: Bearer $GITHUB_TOKEN" \
            "https://api.github.com/repos/${{ github.repository }}/actions/workflows/manual.yml/runs?status=success&per_page=1" \
            | jq -r '.workflow_runs[0].id')

          echo "IOS_RUN_ID=$IOS_RUN_ID" >> $GITHUB_ENV
          echo "ANDROID_RUN_ID=$ANDROID_RUN_ID" >> $GITHUB_ENV
          echo "MANUAL_RUN_ID=$MANUAL_RUN_ID" >> $GITHUB_ENV

          echo "=============================="
          echo "iOS Run ID: $IOS_RUN_ID"
          echo "Android Run ID: $ANDROID_RUN_ID"
          echo "Manual Run ID: $MANUAL_RUN_ID"
          echo "=============================="

      # ==========================================
      # DOWNLOAD IOS
      # ==========================================
      - name: Download iOS report
        run: |
          mkdir -p merged/ios

          curl -s -H "Authorization: Bearer $GITHUB_TOKEN" \
            https://api.github.com/repos/${{ github.repository }}/actions/runs/${IOS_RUN_ID}/artifacts > ios.json

          IOS_ARTIFACT_ID=$(jq -r '.artifacts[] | select(.name=="ios-serenity-json") | .id' ios.json)

          curl -L -H "Authorization: Bearer $GITHUB_TOKEN" \
            -o ios.zip \
            https://api.github.com/repos/${{ github.repository }}/actions/artifacts/$IOS_ARTIFACT_ID/zip

          unzip -o ios.zip -d merged/ios

      # ==========================================
      # DOWNLOAD ANDROID
      # ==========================================
      - name: Download Android report
        run: |
          mkdir -p merged/android

          curl -s -H "Authorization: Bearer $GITHUB_TOKEN" \
            https://api.github.com/repos/${{ github.repository }}/actions/runs/${ANDROID_RUN_ID}/artifacts > android.json

          ANDROID_ARTIFACT_ID=$(jq -r '.artifacts[] | select(.name=="android-serenity-json") | .id' android.json)

          curl -L -H "Authorization: Bearer $GITHUB_TOKEN" \
            -o android.zip \
            https://api.github.com/repos/${{ github.repository }}/actions/artifacts/$ANDROID_ARTIFACT_ID/zip

          unzip -o android.zip -d merged/android

      # ==========================================
      # DOWNLOAD MANUAL
      # ==========================================
      - name: Download Manual report
        run: |
          mkdir -p merged/manual

          curl -s -H "Authorization: Bearer $GITHUB_TOKEN" \
            https://api.github.com/repos/${{ github.repository }}/actions/runs/${MANUAL_RUN_ID}/artifacts > manual.json

          MANUAL_ARTIFACT_ID=$(jq -r '.artifacts[] | select(.name=="manual-serenity-json") | .id' manual.json)

          curl -L -H "Authorization: Bearer $GITHUB_TOKEN" \
            -o manual.zip \
            https://api.github.com/repos/${{ github.repository }}/actions/artifacts/$MANUAL_ARTIFACT_ID/zip

          unzip -o manual.zip -d merged/manual

      # ==========================================
      # MERGE RESULTS
      # ==========================================
      - name: Merge all JSON results
        run: |
          mkdir -p merged/all

          find merged/ios -name "*.json" -exec cp {} merged/all/ \;
          find merged/android -name "*.json" -exec cp {} merged/all/ \;
          find merged/manual -name "*.json" -exec cp {} merged/all/ \;

          echo "Merged JSON files:"
          find merged/all -name "*.json"

      # ==========================================
      # GENERATE REPORT
      # ==========================================
      - name: Generate final Serenity report
        run: |
          mvn serenity:aggregate \
            -Dserenity.outputDirectory=target/final-report

      # ==========================================
      # UPLOAD
      # ==========================================
      - name: Upload final unified report
        uses: actions/upload-artifact@v4
        with:
          name: FINAL-SERENITY-REPORT
          path: target/final-report